# AGENTS.md

## 项目协作规则

- 默认使用中文沟通，回答要直接、简洁、可执行。
- 代码任务先读真实代码路径再判断，不要只基于描述猜测。
- 修改前确认影响面，保持 diff 窄范围，避免顺手重构无关模块。
- 不要覆盖、回滚或提交用户已有的未提交改动；只处理本次任务相关文件。
- 用户要求“列执行清单”时，先给清单，等确认后再改。
- 用户要求“推送”时，先构建验证，再提交并推送到 GitHub。

## tapnow-base 工程规则

- 仓库路径通常是 `D:\无限画布kc\tapnow-base`。
- 本地分支通常是 `master`，部署分支是远端 `origin/main`；推送使用 `git push origin HEAD:main`。
- 标准验证命令是 `npm run build`。
- `npx tsc --noEmit` 可能暴露历史遗留错误；如运行失败，需要区分本次新增错误和既有错误。
- 提交前用 `git status --short --branch`、`git diff --check` 和暂存区 diff 确认范围。
- 不要把 `.env`、后端配置、临时日志、无关 Worker/Server 改动混入 UI 提交。

## UI 修改约束

- 保持现有主题风格，主强调色优先沿用 `#4446CE` 系列。
- 中文文案必须保持 UTF-8，不要引入乱码或替换错文字。
- UI 改动要验证实际渲染，不只看代码；涉及画布、节点、弹窗、下拉框时优先做浏览器交互验证。
- 不影响其他节点和功能模块是硬约束；修改共享组件时必须回归相关入口。
- 截图驱动的 UI 修改要优先匹配用户标注的位置、对齐方式、层级和文案；不要把控件放到相邻但不同的区域。
- 用户明确要求保留“原来的提示文案”时，只改展示样式和位置，不重写业务文案。
- 用户要求“先模拟”或“后端不用改”时，只做前端模拟或前端触发条件，不改接口契约、服务端逻辑或真实审核流程。
- 图片/视频节点的功能改版必须保留既有交互、状态字段和参数映射；提交前要核对请求体或 handler 链路，不能只验证界面长相。
- 涉及审核失败、生成失败、hover、复制报错等体验时，页面默认展示简短中文原因，完整英文/原始错误只放到复制入口或技术排查入口。
- 画布节点输入区、富文本、textarea、input 必须阻断画布级删除、拖拽、快捷键等副作用。
- 输入框悬浮应显示文本光标，不能继承画布 `grab` 手掌光标。
- Backspace/Delete 在输入区域内只能编辑文本，不能删除节点或连线。
- 控件位置、按钮名称、显示/隐藏逻辑要按用户指定精确落地，不自行扩展产品范围。

## 验证与交付

- 窄修改至少执行 `npm run build`。
- UI/交互修复要补充实际浏览器验证结果，例如按钮是否出现、输入是否正常、节点是否保留。
- 如果用户明确说“这次先不用验证”，可以跳过浏览器验证，但仍应做最低成本的代码范围检查；若同时要求推送，仍需按推送流程构建、提交、推送。
- 最终回复说明修改点、验证结果、未完成或未能验证的风险。
- 如果用户要求推送，最终回复包含 commit hash 和推送目标。
